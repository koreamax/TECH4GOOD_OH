# 작업 분배 · 일정

레인 단위로 나눈다. 오너 미정 레인은 팀 회의에서 배정할 것.

## 레인 M — 모델 (오너: 조대흠)

- [ ] v2 크롭 증강 재학습 완료 확인 (`gaero/out/runs/surface_4cls_v2`)
- [ ] v1·v2 외부 평가(Surface_2)로 개선 폭 측정 — 발표용 수치 확보
- [ ] `best.pt` 를 `server/models/` 에 배치, `MODEL_PATH` 설정 → `/infer` 실사진 검증
- [ ] 판정 임계값(ALERT_CONFIDENCE_THRESHOLD) 실측 기반 튜닝
- [ ] (로드맵) RN 온디바이스: `ml/export_tflite.py` 변환 — 데모 필수 아님

## 레인 B — 백엔드·배포 (오너: 미정)

- [ ] Supabase 프로젝트 생성 → PostgreSQL URI를 `DATABASE_URL` 로 주입
- [ ] Docker 이미지 빌드 → Cloud Run/Railway 배포 (자동 HTTPS — 인증서 직접 관리 불필요)
- [ ] Gemini API 키 발급 → 배포 env (없어도 템플릿 폴백으로 데모 가능)
- [ ] NCP 지도 인증키(Client ID+Secret) → 배포 env (지도·역지오코딩 공용)
- [ ] 신뢰도 점수 로직 검증 (동일 지점 반복 탐지 시 점수 상승 확인)
- [ ] ⚠️ 업로드 이미지가 컨테이너 디스크에 저장됨 — 재배포 시 소실. 데모는 무방,
      상시 운영하려면 Supabase Storage 전환 필요

## 레인 W — 웹 프론트 (오너: 미정)

- [ ] Vercel 프로젝트 연결 (Root Directory=`web`), `VITE_API_URL` 설정
- [ ] 폰 브라우저 실기기 QA: 카메라 권한(HTTPS)·GPS·진동
- [ ] P0 경로 QA: 산책→탐지→승인→민원 문안→모의 접수→요약
- [ ] 목 탐지 타이밍을 데모 동선에 맞게 조정 (`web/src/detection/mock.ts`)
- [ ] 모델 배포 후 `VITE_USE_SERVER_INFER=true` 전환 테스트

## 레인 P — 기획·발표 (오너: 여아현)

- [ ] 기획서 갱신: 선 승인 게이트 + 민원 접수 단계 + 임계값 알람 정책 반영
- [ ] 기획서 클래스 서술 수정 (damaged 2클래스 → normal 포함 4클래스 학습, 알람은 damaged만)
- [ ] 발표 자료: 반려견 순찰대 20:1 수치, 영조물 배상 2.5배 증가, 콜드스타트 방어(개인 효용) 슬라이드
- [ ] 데모 시나리오 대본 (S-01→S-04 경로, 예상 소요 3분)
- [ ] 안전신문고 웹 신고 플로우 1회 수동 검증 (로그인 세션만으로 제출 가능한지) — 발표 Q&A 대비

## 일정 (역산)

| 단계 | 내용 | 완료 기준 |
|------|------|-----------|
| 지금 | v2 학습 중 + 앱/서버 스캐폴드 완료 | 목 모드로 전체 플로우 동작 |
| 모델 완료 후 | TFLite 변환·탑재, 임계값 튜닝 | 실기기 온디바이스 탐지 |
| 데모 D-1 | P0 리허설 2회, 폴백 결정 | 3분 시나리오 무중단 |
| 데모 D-Day | 시연망 구성, 목/실모델 최종 선택 | — |

## 컷라인 정책 (일정 밀릴 때 자르는 순서)

1. 기록 상세(S-21) → 2. 서버 YOLO 추론(목 모드로 데모) → 3. Gemini(템플릿 폴백) → 4. 클라우드 배포(로컬+터널로 대체)

**절대 안 자르는 것:** 산책→탐지→승인→민원 문안→모의 접수 경로, 경로 시각화.

## 브랜치 규칙

- 공유 브랜치 = `develop`. **직접 커밋 금지**, feature 브랜치 → PR.
- 브랜치명: `feat/<영역>-<내용>` (예: `feat/app-walk-screen`), 1 PR = 1 관심사.
